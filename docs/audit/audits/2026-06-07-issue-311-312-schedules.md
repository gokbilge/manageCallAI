# Audit - issue-311-312-schedules - 2026-06-07

**Commit:** `8322ebf`
**Scope:** Schedule-group, holiday-calendar, and temporary override workflow changes for issues `#311` and `#312`
**Result:** PASS

## Findings

No open findings.

## Validation

- `pnpm --filter @managecallai/contracts build`
- `pnpm --filter @managecallai/api test -- schedule.service.test.ts schedule.util.test.ts`
- `pnpm --filter @managecallai/api exec eslint src/modules/schedules/*.ts src/modules/ivr-flows/ivr-flow.service.ts src/modules/ivr-flows/ivr-flow.repository.ts src/modules/runtime/ivr-runtime.service.ts src/modules/runtime/ivr-runtime.repository.ts src/modules/repository-coverage.test.ts`
- `pnpm --filter @managecallai/web test -- schedules-page.test.tsx`
- `pnpm --filter @managecallai/web build`
- `pnpm generate:openapi`
- `pnpm generate:web-types`

## Notes

- DB-backed validation was partially blocked in this environment because PostgreSQL was not reachable on `localhost:5432` and Docker Desktop was not running, so `pnpm db:migrate` and the new schedule integration test file could not be executed locally.
- Full API `tsc` for the entire workspace is also blocked by unrelated local, untracked `enterprise-lifecycle` files that are outside the scope of this change and are not included in the staged commit.
