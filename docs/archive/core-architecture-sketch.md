> Superseded by `docs/architecture/source-of-truth.md` as the canonical architecture reference.
>
> This file previously contained an early architecture sketch with broken character encoding. Keep it only if a cleaned version is needed later, but do not treat it as the authoritative project design document.
